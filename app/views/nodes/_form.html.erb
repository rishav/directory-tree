
<%= form_for [tree, node] do |f| %>
  <% if parent %>
      <%= f.hidden_field :parent_id, value:parent.id %>
  <% end %>
      <%= f.text_field :name, placeholder: 'node name' %>
  <%= f.submit %>
<% end %>
