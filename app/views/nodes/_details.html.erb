<div class="node">
  <%= node.name %>
  <div class="node-children">
    <% node.children.each do |node| %>
      <%= render partial: 'nodes/details', locals: {node: node} %>
    <% end %>
  </div>
</div>
